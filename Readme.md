# Shell Script Assignment
# Description:
     ShellScript.sh contains the Script 
     input.csv contains the data on which the script has been tested
     output.csv contains the result of the script.
# To run the file:
    Step 1 : Make the script executable by using the command 
            chmod u+x ShellScript.sh
    Step 2 : Run the script by using the command
            ./ShellScript.sh
(Note : Make sure to Replace the input and output path of the file in the main function of the Script)

